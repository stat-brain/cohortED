#' Perform Parallel Cohort Analysis
#' 
#' @title Parallel Cohort Analysis
#' @description Makes a plot of parallel cohorts
#' 
#' @param YEARS Vector indicating which years to include in the analysis
#' @param START_GRADE Integer indicating which grade level to start with
#' @param CONTENT Character string indicating which content area to work with
#' 
#' @return A list containing the greeting and optional metadata
#' @export
#' 
#' 

parallel_cohort = function(YEARS, START_GRADE, CONTENT) {
  BASELINE = sort(YEARS)[1]
  
  
  
  
  return(BASELINE)
}
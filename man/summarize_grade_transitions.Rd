% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize_grade_changes.R
\name{summarize_grade_transitions}
\alias{summarize_grade_transitions}
\title{Summarize Student Grade Transitions and Enrollment Patterns}
\usage{
summarize_grade_transitions(dataset)
}
\arguments{
\item{dataset}{A data frame containing at least the columns \code{ID}, \code{GRADE}, and \code{YEAR}.}
}
\value{
A named list invisibly containing:
\itemize{
\item \strong{Student_Transitions}: A cleaned student-level dataset with derived columns:
\itemize{
\item \code{GRADE_CHANGE}: Numeric change in grade from prior year
\item \code{YEAR_CHANGE}: Numeric change in year
\item \code{GRADE_GAP}: Grade change minus year change
\item \code{GRADE_TRANSITION}: Categorized as \code{"Stay"}, \code{"Repeat"}, \code{"Skip"}, or \code{"Drop_Back"}
\item \code{ENROLLMENT_TRANSITION}: \code{"Enrolled"} or \code{"Return"} based on year continuity
\item \code{IS_FINAL}: Marks final row for each student
\item \code{ACADEMIC_YEAR}: Computed label from \code{YEAR}
}
\item \strong{Summary_Tables}: A list of summary tables:
\itemize{
\item \code{By_Grade}: Count of each grade transition type
\item \code{By_Year_And_Grade}: Grade transitions by academic year (excluding first year)
\item \code{By_Year_And_Enrollment}: Enrollment status by academic year
\item \code{Note}: Explains why no transitions are shown for the first observed year
}
\item \strong{Details}:
\itemize{
\item \code{Conflicts}: Student-year records with multiple grade levels (if any)
\item \code{Enrollment_Transitions}: Total counts of \code{"Enrolled"} and \code{"Return"} statuses
\item \code{Summary_Tables$By_Year_And_Enrollment}: Detailed year-level enrollment summary
\item \code{Notes}: Informational messages about the return group (if applicable)
}
\item \strong{Data}:
\itemize{
\item \code{Return}: Subset of records for students who returned after missing years
\item \code{Stay}, \code{Repeat}, \code{Skip}, \code{Drop_Back}: Subsets of records based on grade transition type
}
}
}
\description{
Identifies and summarizes student grade-level transitions across academic years.
This function classifies transitions such as grade repetition, Skipping, and dropping back,
and also detects non-contiguous enrollment patterns (returns).
}
\details{
Grade transitions are classified relative to expected year-over-year progress.
A student who Skips a grade (e.g., from 3 to 5) is labeled as \code{"Skip"}, while a student
who Repeats a grade (e.g., 3 to 3) is labeled as \code{"Repeat"}. A \code{"Return"} enrollment
transition indicates a break in year continuity (e.g., a student is observed in 2019 and 2021 but not 2020).

Grade transitions are not reported for the first observed year in the dataset, as there is no
prior year for comparison.
}
\examples{
summarize_grade_transitions(dataset = math)

}
